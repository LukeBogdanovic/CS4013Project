public class Statistics {

    // returns total tax paid for an area specified by eircode key routing
    public double totalTaxPaid(String eC) {
        double tTp = 0;
        String[] key = eC.split("_");
        for (Property p : properties) {
            if (key[1].equals()) {

            }
        }
        return tTp;
    }

}
